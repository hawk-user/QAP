name: Functional Testing On The Web Application

on:
  pull_request:
    paths:
      - "webapp/**"
      - "test/system/functional/webapp/**"
      - "libs/react-materials/**"
      - "test/component/libs/react-materials/**"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  test-local:
    uses: ./.github/workflows/run.functional.tests.on.the.web.application.with.cypress.yml
    with:
      start_local: true
